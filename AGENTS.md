# Project Rules

- When asked to create a new `research.md` or `plan.md`, always delete any existing contents first and create the file inside the `docs/` directory.
- When creating a new `plan.md`, always make the final plan step include writing the PR according to the repository PR template.
